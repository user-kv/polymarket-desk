# Runtime deps for the desk + papertrader when hosted (GitHub Actions / VPS).
# Deliberately tiny: the whole system is standard-library-first.
google-generativeai>=0.8.0   # the free self-learning brain (Gemini)
openpyxl>=3.1.0              # papertrader tracker.xlsx writer
# anthropic / openai / prefect are intentionally NOT here — they are lazy-imported
# and only needed if you deliberately switch DESK_LLM to a paid backend.
